name: OPA Policy Check

on:
  pull_request:
    branches: [ main ]

jobs:
  opa:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install OPA and Conftest
        run: |
          curl -L -o opa https://openpolicyagent.org/downloads/latest/opa_linux_amd64_static
          chmod +x opa
          sudo mv opa /usr/local/bin/opa
          curl -L -o conftest.tar.gz https://github.com/open-policy-agent/conftest/releases/latest/download/conftest_Linux_x86_64.tar.gz
          tar -xzf conftest.tar.gz
          sudo mv conftest /usr/local/bin/conftest

      - name: Run policy checks
        run: |
          # Example: run OPA evaluation or Conftest tests on your Terraform plan or Kubernetes manifests
          # This is a placeholder; replace with actual commands to evaluate policies against your infrastructure code.
          echo "OPA and Conftest installed; add commands to evaluate your policies here."
